import React from "react";
const Footer = ({className='footer', ...props}) => {
    return(
        <div
            className={className}
            {...props}
        >
            Footer
        </div>
    )
}

export default Footer;